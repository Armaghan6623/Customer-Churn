version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - echo Building Docker Image...
      - docker build -t churn-app .

# 3. Create a placeholder file so the models folder shows up
!touch models/.gitkeep

# 4. The Strict Git Routine: Add, Commit, and Push
!git add buildspec.yml models/
!git commit -m "feat: add buildspec for AWS and models directory placeholder"
!git push origin main
