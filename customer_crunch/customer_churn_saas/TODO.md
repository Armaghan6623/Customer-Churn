# customer_churn_saas

Optional Streamlit app (churn profiling + macro forecasting). The primary HF Space deployment uses the root `Dockerfile` and Gradio UI in `customer crunch/ui/app.py`.

- [x] Package layout under `src/customer_churn_saas`
- [x] automation.py, preprocessing.py, model.py, app.py
- [x] GitHub workflow `hf_deploy.yml` (root Gradio deployment)
- [ ] Optional: deploy via `python ../../scripts/deploy_hf.py` with `HF_TOKEN` set

