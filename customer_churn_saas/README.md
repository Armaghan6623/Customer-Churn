# customer_churn_saas

## Run Streamlit
From this folder:

```bash
streamlit run app.py
```

## Notes
- The package code lives under `src/customer_churn_saas/`.
- Model/plots are expected to exist under `saved_models/` in the repo root.

